/**
 * Test for TheUserRoleResource.
 * Runs with mocha.
 */
'use strict'

const TheUserRoleResource = require('../lib/TheUserRoleResource')
const { ok, equal } = require('assert')

describe('the-user-role-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheUserRoleResource)
  })
})

/* global describe, before, after, it */
